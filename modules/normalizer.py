class Normalizer:
    def normalize(self, param_autos):
        # take param_autos dict and make sure the keys are normalised before doing comparison with param_inputs
        print("Normalizing test names")
        return param_autos